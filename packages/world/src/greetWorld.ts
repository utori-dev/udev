import { logGreeting } from '@utori-dev/template-typescript-monorepo-hello';

/**
 * Logs a greeting that says 'Hello, World!'
 */
function greetWorld(): void {
  logGreeting('World');
}

export default greetWorld;
