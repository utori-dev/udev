/**
 * Create a greeting that says 'Hello, `name`!'
 *
 * @param name Name of individual or group to greet
 */
function createGreeting(name: string): string {
  return `Hello, ${name}!`;
}

export default createGreeting;
