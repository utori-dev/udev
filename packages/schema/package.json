{
  "name": "@udev/schema",
  "version": "1.0.0",
  "description": "JSON schema for the @udev tooling",
  "license": "MIT",
  "main": "dist/index.js",
  "module": "umd",
  "scripts": {
    "build": "tsc --project tsconfig.build.json",
    "clean": "rimraf dist/",
    "lint": "prettier --write src",
    "test": "jest"
  }
}
